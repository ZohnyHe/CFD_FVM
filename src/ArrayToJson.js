let timeStep = [0, 0.5, 0.75, 1.5]

// let JsonArray = JSON.stringify(MyArray)

console.log(JsonArray)

class TimeData {
    constructor(timeStep) {
        this.timeStep = timeStep;
    }
}

var a = new TimeData

for (let i = 0; i < OpenFoamArray.length; i = i+3) {
    OpenFoamArray
}